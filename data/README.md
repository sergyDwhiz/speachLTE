# Data Directory

This folder holds dataset artifacts at different processing stages.

- `raw/`: original audio and transcripts as collected.
- `interim/`: intermediate cleaned or segmented outputs.
- `processed/`: final features, manifests, and token files ready for training.

Keep personally identifiable information out of this repository and document consent for every dataset stored here.
